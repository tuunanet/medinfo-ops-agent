{
  "defect_class": "bootstrap-only-container-credential-with-persistent-state",
  "grep_pattern": "POSTGRES_PASSWORD|MYSQL_ROOT_PASSWORD|MONGO_INITDB_ROOT_PASSWORD|POSTGRES_HOST_AUTH_METHOD",
  "match_count": 1,
  "sweep_scope": "scripts/ apps/ services/",
  "patched_in_pr": [
    "scripts/database-container.sh"
  ],
  "tracked_issues": [],
  "notes": "The only persistent-service bootstrap credential is now validated and synchronized to the preserved local role before readiness is reported."
}
